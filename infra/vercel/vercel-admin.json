{
  "version": 2,
  "builds": [
    {
      "src": "frontend/admin-dashboard/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [{ "src": "/admin/(.*)", "dest": "https://admin.aigestion.net/$1" }]
}
