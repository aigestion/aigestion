import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aigestion.pixel7a.showcase',
  appName: 'Pixel 7a Sovereign',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
