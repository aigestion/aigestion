name: Auto Rollback

on:
  workflow_dispatch:
  deployment_status:

jobs:
  rollback:
    if: github.event.deployment_status.state == 'failure'
    runs-on: ubuntu-latest
    steps:
      - name: Rollback
        run: echo "Rolling back to previous revision..."
