name: Compliance Header Check

on: [pull_request]

jobs:
  headers:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify Headers
        run: |
          grep -r "Copyright" src/ || echo "Warning: No copyrights found"
