---
description: Run the comprehensive test suite for all components
---

# Run Tests Workflow

This workflow executes all unit, integration, and e2e tests across the repository.

## Steps

### 1. Preparation
// turbo
1. Install dependencies.
   ```bash
   pnpm install --frozen-lockfile
   ```
// turbo
2. Setup environment.
   ```bash
   cp .env.example backend/.env
   ```

### 2. Linting
// turbo
3. Run linting on all packages.
   ```bash
   pnpm lint
   ```

### 3. United & Integration Tests
// turbo
4. Run backend tests.
   ```bash
   cd backend
   pnpm test
   ```
// turbo
5. Run frontend tests.
   ```bash
   cd frontend
   pnpm test
   ```

### 4. Security Scan
// turbo
6. Run local security audit.
   ```bash
   pnpm audit
   ```

---
*Generated by Antigravity - God Mode Optimization*
