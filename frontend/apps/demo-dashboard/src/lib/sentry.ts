/**
 * 🌌 Sentry God Mode — demo-dashboard
 * MUST be imported as first module in main.tsx
 */
import { initSentry } from '@aigestion/sentry-config';

initSentry({ appName: 'demo-dashboard', version: '2.0.0' });
