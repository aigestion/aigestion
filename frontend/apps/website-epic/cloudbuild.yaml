steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', '$_IMAGE_NAME', '-f', 'frontend/apps/website-epic/Dockerfile', '--network=cloudbuild', '.']
images:
  - '$_IMAGE_NAME'
options:
  machineType: 'E2_HIGHCPU_8'
